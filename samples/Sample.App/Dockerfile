FROM mcr.microsoft.com/dotnet/runtime:10.0 AS base
WORKDIR /app

USER app
FROM --platform=$BUILDPLATFORM mcr.microsoft.com/dotnet/sdk:10.0 AS build
ARG configuration=Release
WORKDIR /src
COPY ["samples/Sample.App/Sample.App.csproj", "samples/Sample.App/"]
RUN dotnet restore "samples/Sample.App/Sample.App.csproj"
COPY . .
WORKDIR "/src/samples/Sample.App"
RUN dotnet build "Sample.App.csproj" -c $configuration -o /app/build

FROM build AS publish
ARG configuration=Release
RUN dotnet publish "Sample.App.csproj" -c $configuration -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "Sample.App.dll"]
